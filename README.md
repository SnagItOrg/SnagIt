# PAN-86 review screenshots — disposable branch

Images referenced from the PAN-86 pull request body. This branch exists only
so GitHub can render them inline; it holds no code, is never merged, and can
be deleted the moment the PR is closed.
